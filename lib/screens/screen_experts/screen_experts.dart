import 'package:flutter/material.dart';

class ScreenExperts extends StatelessWidget {
  static String route = '/experts';

  const ScreenExperts({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Experts screen");
  }
}
